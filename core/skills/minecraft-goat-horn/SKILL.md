---
name: minecraft-goat-horn
description: |
  山羊角乐器定义格式（Minecraft Wiki 中文版全量正文）。
  
  【概述】本条目介绍的是数据包中定义山羊角乐器的格式。关于原版中的山羊角乐器，请见“山羊角 § 物品数据”。
  
  【涵盖内容】
  - （自动提取章节）
  
  【关键定义】
  - 注册表：INSTRUMENT
  
  【适用场景】编写数据包 / 资源包 / Java 版自定义内容，需要 山羊角乐器定义格式 的完整规范时
---

本条目介绍的是数据包中定义山羊角乐器的格式。关于原版中的山羊角乐器，请见“山羊角 § 物品数据”。

本条目所述内容仅适用于Java版。
山羊角乐器，也作乐器（Instrument），决定了玩家吹奏山羊角的行为。山羊角乐器定义文件是山羊角乐器在数据包中的数据驱动定义文件。

# 定义格式

山羊角乐器定义数据在游戏内使用
```
INSTRUMENT
```

注册表，数据包路径为
```
instrument
```

，即所有山羊角乐器定义文件都需要在
```
data/<
命名空间
>/instrument
```

目录内定义，山羊角乐器标签则需要在
```
data/<
命名空间
>/tags/instrument
```

目录内定义。

山羊角乐器定义文件使用JSON格式，并具有下列结构：

- [图:NBT复合标签/JSON对象] JSON文件根对象 - [图:字符串][图:NBT复合标签/JSON对象][图:NBT列表/JSON数组]*description：（文本组件）乐器的名称。 - [图:字符串][图:NBT复合标签/JSON对象]*sound_event：吹奏时播放的声音事件。可以为一个声音事件注册名，或直接定义声音事件的详细数据。 - - 声音事件，见Template:Nbt inherit/sound event/source - [图:整型]durability_damage：（值≥0）玩家每吹奏一次后，物品消耗的耐久度。 - [图:单精度浮点数]*range：（值>0）吹奏声音能传播的最远距离。 - [图:单精度浮点数]*use_duration：（值>0值≥0）吹奏时间，单位为秒，影响物品冷却速度。

# 定义行为

山羊角乐器定义数据仅在服务端启动时被加载一次，使用
```
/
reload
```

命令不可以使山羊角乐器定义被重新加载，而必须重启服务端。

当玩家吹奏带有
```
instrument
```

物品堆叠组件的山羊角时，游戏会读取组件内的山羊角乐器信息，并进行播放。

山羊角播放的声音归属
```
record
```

（唱片机/音符盒）分类，声音实例音量为[图:单精度浮点数]*range的16分之一，实例音高为1，立刻播放，音量线性衰减。玩家能听到山羊角声音的最远距离（假设声音播放期间没有移动）为声音事件引用[图:字符串]sound_id定义的[图:整型]attenuation_distance值、山羊角乐器定义内的[图:单精度浮点数]*range值和声音事件中的[图:单精度浮点数]range值中的最小值。

# 历史

# 导航
